apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hw02chart.fullname" . }}-test-connection"
  labels:
    {{- include "hw02chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hw02chart.fullname" . }}:{{ .Values.service.port }}/myactuator/isready']
  restartPolicy: Never
